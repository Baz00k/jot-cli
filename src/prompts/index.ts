import editor from "./editor.md" with { type: "file" };
import reviewer from "./reviewer.md" with { type: "file" };
import writer from "./writer.md" with { type: "file" };

export const promptPaths = {
    editor,
    reviewer,
    writer,
};
